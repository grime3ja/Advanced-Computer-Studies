## Group Project backend code (Android App)
- This is not a runnable app because I lost the app code, but the stuff that happens in the backend is here

###
- src/Test.java
    - Set up the 2D array used in the finding of classes
- src/ClassLocation.java
    - Do the searching using Dijkstra's algorithm