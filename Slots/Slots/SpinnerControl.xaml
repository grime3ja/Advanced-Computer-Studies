<UserControl x:Class="Spinner.SpinnerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded">
    <Border Name="border" BorderBrush="Black" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Name="textBox" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"
                     VerticalContentAlignment="Center" TextAlignment="Right" Margin="0, 0, 2, 0" BorderThickness="0"
                     Text="{Binding ControlValue, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                     Height="{Binding ControlHeight, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                     Width="{Binding ControlWidth, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>

            <Button Name="up" Grid.Row="0" Grid.Column="1" 
                    Height="{Binding ArrowHeight, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" 
                    Width="Auto" Click="UpArrow_Click">
                <Image Source="UpArrow.png"/>
            </Button>
            <Button Name="down" Grid.Row="1" Grid.Column="1" 
                    Height="{Binding ArrowHeight, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" 
                    Width="Auto" Click="DownArrow_Click">
                <Image Source="DownArrow.png" />
            </Button>
        </Grid>
    </Border>
</UserControl>